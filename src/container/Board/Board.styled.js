import styled from "styled-components";

const SBoard = styled.section`
  user-select: none;
  white-space: nowrap;
  margin-bottom: 8px;
  overflow-x: auto;
  overflow-y: hidden;
  padding-bottom: 8px;
  position: absolute;
  top: 70px;
  height: 90vh;
  right: 0;
  bottom: 0;
  left: 0;
`;

export default SBoard;
